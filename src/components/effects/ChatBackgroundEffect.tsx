
import React from "react";

interface ChatBackgroundEffectProps {
  isDarkMode: boolean;
}

export const ChatBackgroundEffect: React.FC<ChatBackgroundEffectProps> = () => {
  // Empty component, background removed
  return null;
};
