
import React from 'react';

export const HoveredLink = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export const Menu = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export const MenuItem = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export const ProductItem = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default {
  HoveredLink,
  Menu,
  MenuItem,
  ProductItem,
};
