
import React from 'react';

interface ARBackgroundProps {
  scrollY: number;
}

const ARBackground: React.FC<ARBackgroundProps> = () => {
  // Empty component, background removed
  return null;
};

export default ARBackground;
